backend = "glx";
paint-on-overlay = true;
glx-no-stencil = true;
glx-copy-from-front = false;
vsync = true;

opacity-rule = [
  "85:class_g = 'Emacs' && focused",
  "85:class_g = 'Emacs' && !focused",
#  "90:class_g = 'Zathura' && focused",
#  "90:class_g = 'Zathura' && !focused"
];
